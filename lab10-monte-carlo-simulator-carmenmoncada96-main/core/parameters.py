# Use this file to define all your input parameters (e.g. lengths, loss coefficient, etc.)
# c = speed of light
c = 3 * (10 ** 8) # 3e8

# signal information
signal_power = 1e-3

# Number of connections
connections_number = 100

# BIT_RATE requested for all pairs of nodes in the uniform matrix
# Look in utils.py to see the function where this variable is used
BIT_RATE_REQUEST = 100e9  # Gbps
M = 10  # is an increasing integer number (1, 2, 3, ...).
